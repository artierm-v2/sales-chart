import { Routes } from '@angular/router';
import { SalesChartComponent } from './features/sales-chart/sales-chart.component';

export const routes: Routes = [
  { path: '', component: SalesChartComponent },
];
